import React from 'react';
// import DashboardLoading from 'core/layout/dashboardLoading';
// import { Await, useLoaderData } from 'react-router-dom';
// import { TournamentForm } from './tournamentForm';
// import { type TournamentFormType } from '../models/tournamentForm.model';

export function EditTournament (): JSX.Element {
  // const { tourney, ...props } = useLoaderData() as { tourney: TournamentFormType };
  return <></>;

  // return <Suspense fallback={<DashboardLoading />}>
  //   <Await
  //     resolve={tourney}
  //   >
  //     {(tourney) => (<TournamentForm tourney={tourney}/>)}
  //   </Await>
  // </Suspense>;
};
